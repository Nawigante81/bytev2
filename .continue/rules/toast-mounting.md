---
description: Prevents missing toast UI when using toast() calls in pages and components.
alwaysApply: false
---

Always mount a global <Toaster /> component once near the root (e.g., in App.jsx) whenever using the custom toast/use-toast utilities.