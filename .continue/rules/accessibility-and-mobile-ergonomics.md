---
globs: src/**/*.{jsx,tsx,css}
description: Apply to layout and pages when changing navigation, focus states,
  or adding anchors.
alwaysApply: true
---

Always add visible :focus-visible styles, a skip link to main content, and ensure sticky headers don't cover anchor targets using scroll-margin-top.